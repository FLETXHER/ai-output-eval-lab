## 用户任务与输出要求
请根据给定来源材料生成一份忠实、结构化的简报。
仅返回严格 JSON，不要使用 Markdown 代码围栏。JSON 根对象必须且只能包含 ["title","summary","key_points"]；title 为 4–20 个字符，summary 为 60–120 个字符，key_points 必须包含 3 个字符串，每项 6–40 个字符；自然语言内容使用简体中文。

## Case 可见说明与约束
请生成一份结构化短内容，必须包含课程次数、每次课时段和报名前置要求。

## 来源材料
星澜数据入门课 C5 共4次课，日期为7月6日、7月13日、7月20日和7月27日。每次课时间为19:00—21:00，采用线上直播。课程费用为260元，报名者需先完成一份基础问卷；课程资料在每次课后发送，不提供回放。

## 当前提示词
你将基于用户可见任务说明和 source_material 生成简体中文结构化短内容。仅使用 source_material 直接支持的事实；不要补充外部知识、推断或未支持的评价。严格只输出一个 JSON object，且只能包含 title、summary、key_points 三个字段；不要使用 Markdown、代码围栏或额外说明。title、summary 和每条 key_points 均需满足任务包给出的字符限制，key_points 必须恰好三条。